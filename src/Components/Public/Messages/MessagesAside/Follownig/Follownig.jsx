import React from 'react';
import f from './Follownig.module.css';


const Follownig = () => {
	return (
		<div className={f.follownig}>
			follownig
		</div>
	);
}

export default Follownig;